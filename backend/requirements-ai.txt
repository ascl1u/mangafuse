# MangaFuse — Phase 2 AI dependencies (CPU-only)
#
# Notes:
# - Install Torch/Torchvision CPU wheels using the CPU index URL:
#   pip install --index-url https://download.pytorch.org/whl/cpu -r requirements-ai.txt
# - Keep these isolated from Phase 1 runtime deps in backend/requirements.txt

numpy
pillow
opencv-python
shapely

# Segmentation (YOLOv8)
torch
torchvision
ultralytics

# OCR
manga-ocr

# Inpainting
simple-lama-inpainting

# Translation (Gemini via Google AI Python SDK)
google-genai


